# VeerCanvas site templates

Each subdirectory is a theme package copied into `sites/<id>/` when creating a website.

- `registry.json` — catalog metadata (id, layout, siteTypes, preview)
- `<templateId>/` — HTML/CSS/JS (+ assets) scaffolded onto new sites

Create custom templates from the canvas Site Studio (clones an existing package + registry entry).
