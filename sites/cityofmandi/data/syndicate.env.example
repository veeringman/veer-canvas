# Incoming neighbourhood tokens (Bearer). Generated on first hub boot if missing.
# Copy SYNDICATE_TOKEN_HBCSANYARD into housingcolonysanyard data/city-hub.env
# as CITY_HUB_TOKEN when the two sites are not on the same host.
#
# SYNDICATE_TOKEN_HBCSANYARD=
