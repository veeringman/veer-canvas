# Site theme

Copy theme files from `sites/veerlabs/` (index.html, project.html, style.css, *.js) when bootstrapping a new site.

VeerCanvas will eventually provide a theme package manager; for now duplicate the VeerLabs theme and customize branding.
